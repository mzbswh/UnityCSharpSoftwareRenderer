# 快速开始指南

## 步骤1：创建测试场景

1. 在Unity中打开项目 `UnityCSharpSoftwareRenderer`
2. 创建新场景：File → New Scene → Basic (Built-in Render Pipeline)
3. 保存场景为 `SoftwareRendererDemo`

## 步骤2：创建渲染管理器

1. 创建空GameObject：Hierarchy → Create Empty
2. 重命名为 "SoftwareRenderer"
3. 添加组件：Add Component → 搜索 "SoftwareRenderManager"

## 步骤3：创建输出显示

1. 创建UI Canvas：
   - Hierarchy → UI → Canvas
   - 设置Canvas Scaler → UI Scale Mode = Scale With Screen Size
   - Reference Resolution = 1920x1080

2. 在Canvas下创建RawImage：
   - 右键Canvas → UI → Raw Image
   - 设置RawImage为全屏：
     - Anchor Presets → 按住Shift+Alt点击右下角（stretch all）
     - Left/Right/Top/Bottom = 0

3. 将RawImage拖到SoftwareRenderer的 `Output Image` 字段

## 步骤4：添加测试模型

### 方法A：使用Unity原始体

1. 创建一个Cube：GameObject → 3D Object → Cube
2. 创建一个Sphere：GameObject → 3D Object → Sphere  
3. 创建一个Cylinder：GameObject → 3D Object → Cylinder
4. 将这些物体放置在不同位置（例如：Cube在(0,0,0), Sphere在(2,0,0), Cylinder在(-2,0,0)）
5. 选择SoftwareRenderer GameObject
6. 将这些物体的MeshFilter组件拖到 `Test Meshes` 列表中

### 方法B：使用场景中现有模型

如果场景中已有模型：
1. 选择SoftwareRenderer GameObject
2. 增加 `Test Meshes` 列表大小
3. 将现有模型的MeshFilter组件拖入列表

## 步骤5：配置渲染设置

在SoftwareRenderer的Inspector中调整参数：

### 输出设置
- **Render Width**: 1280 (分辨率宽度，建议≤1280以保证性能)
- **Render Height**: 720 (分辨率高度，建议≤720以保证性能)

### 渲染设置
- **Enable MSAA**: ☑ (启用4x抗锯齿，会降低性能但提升画质)
- **Enable Depth Test**: ☑ (启用深度测试，必须开启)
- **Enable Face Culling**: ☑ (启用背面剔除，提升性能)
- **Clear Color**: 黑色或你喜欢的背景色

## 步骤6：运行

1. 点击Play按钮
2. 你应该能在RawImage中看到渲染结果
3. 左上角会显示性能信息：
   - Resolution: 分辨率
   - MSAA: 抗锯齿状态
   - Models: 模型数量
   - FPS: 帧率

## 预期效果

- **简单场景** (1-3个模型): 30-60 FPS (1280x720, 无MSAA)
- **中等场景** (4-10个模型): 15-30 FPS (1280x720, 无MSAA)
- **复杂场景** (>10个模型): <15 FPS (1280x720, 无MSAA)

开启MSAA后帧率会下降约50-70%。

## 性能优化建议

### 如果帧率太低：

1. **降低分辨率**
   - 尝试 640x360 或 800x450

2. **关闭MSAA**
   - 取消勾选 `Enable MSAA`

3. **减少模型数量**
   - 移除一些测试模型

4. **使用简单几何体**
   - Unity原始体（Cube, Sphere）比复杂模型快

### 如果画质不佳：

1. **提高分辨率**
   - 尝试 1920x1080（如果性能允许）

2. **启用MSAA**
   - 勾选 `Enable MSAA`

3. **检查Clear Color**
   - 确保背景色合适

## 常见问题

### Q: 屏幕全黑，什么都看不到
A: 检查：
- RawImage是否正确连接
- Test Meshes列表是否有模型
- 相机位置是否能看到模型（默认在(0,2,5)看向(0,0,0)）

### Q: 帧率太低
A: 降低分辨率或关闭MSAA。软渲染是CPU密集型，性能有限。

### Q: 模型显示不正确
A: 确保：
- Mesh有有效的顶点、法线和UV
- 模型不在相机视锥体外

### Q: 编译错误
A: 检查：
- 所有.cs文件都在正确的文件夹中
- Unity版本≥2020.3（建议）
- 没有命名空间冲突

## 下一步

成功运行基础场景后，你可以：

1. **尝试不同的Shader**
   - 修改Material的shadingModel
   - 切换到BlinnPhong进行光照渲染

2. **添加纹理**
   - 导入纹理到testTextures列表
   - 配置Material使用纹理

3. **调整相机**
   - 修改Camera的Eye/Center/Up位置
   - 改变FOV角度

4. **添加光照**
   - 调整SceneManager的光照参数
   - 设置点光源位置和颜色

5. **实现自定义Shader**
   - 参考BasicShader.cs创建新Shader
   - 注册到材质系统

## 技术支持

遇到问题？
- 查看 `README.md` 了解详细技术文档
- 查看 `IMPLEMENTATION_SUMMARY.md` 了解架构设计
- 检查Console窗口的错误信息

祝你使用愉快！🎨

